@model Quoteversation.Web.ViewModels.Tags.AllTagsViewModel

@foreach (var tag in Model.Tags)
{
    @Html.ActionLink(tag.Name, "ById", "Tags", new { id = tag.Id, convId = ViewData["convId"] }, new { style = "color: inherit; margin: 5px", @class = "btn btn-info" })
}